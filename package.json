{
  "name": "donut-man",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:silberGuy/donut-man.git",
  "author": "guy silberman <guysilberman1@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "parcel build src/index.html",
    "serve-dev": "parcel src/index.html",
    "deploy": "rm -rf dist && yarn build && firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.11"
  },
  "dependencies": {
    "three": "^0.119.1"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
